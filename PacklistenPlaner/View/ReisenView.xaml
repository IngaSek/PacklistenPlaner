<UserControl x:Class="PacklistenPlaner.ReisenView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PacklistenPlaner"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="800">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <ToggleButton Command="{Binding ReiseHinzufuegenCommand}">Neue Reise</ToggleButton>
            <ToggleButton Command="{Binding ReiseBearbeitenCommand}">Reise bearbeiten</ToggleButton>
            <ToggleButton Command="{Binding ReiseEntfernenCommand}">Reise entfernen</ToggleButton>
        </ToolBar>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <DataGrid ItemsSource="{Binding Reisen}" SelectionMode="Single" SelectedItem="{Binding SelectedReise}" AutoGenerateColumns="False" IsReadOnly="True" Margin="2" FontSize="14">
                <DataGrid.RowStyle>
                    <Style TargetType="{x:Type DataGridRow}">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FFAAAAAA"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="{x:Type DataGridCell}">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FFD0D0D0"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="BorderBrush" Value="#FFD0D0D0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Titel" Binding="{Binding Titel}" Width="4*"/>
                    <DataGridTemplateColumn Header="Startdatum" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <DatePickerTextBox Text="{Binding Startdatum, StringFormat='{}{0:dd.MM.yyyy}'}" IsReadOnly="True">
                                    <DatePickerTextBox.Style>
                                        <Style TargetType="DatePickerTextBox">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridCell}}" Value="True">
                                                    <Setter Property="Background" Value="#FFD0D0D0"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DatePickerTextBox.Style>
                                </DatePickerTextBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Enddatum" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <DatePickerTextBox Text="{Binding Enddatum, StringFormat='{}{0:dd.MM.yyyy}'}" IsReadOnly="True">
                                    <DatePickerTextBox.Style>
                                        <Style TargetType="DatePickerTextBox">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridCell}}" Value="True">
                                                    <Setter Property="Background" Value="#FFD0D0D0"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DatePickerTextBox.Style>
                                </DatePickerTextBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Tage" Binding="{Binding Tage}" Width="1.1*"/>
                    <DataGridTextColumn Header="Geplant von" Binding="{Binding Planender.Name}" Width="2*"/>
                    <DataGridTemplateColumn Header="Status" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="{Binding Status, Mode=OneWay}" Minimum="0" Maximum="100" Margin="1" Height="20" VerticalAlignment="Top"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Beschreibung" Width="5.5*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Beschreibung}" TextWrapping="Wrap" MaxHeight="80"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </DockPanel>
</UserControl>
